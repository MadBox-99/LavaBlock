-- A glazed column on a steel frame: what algae is actually grown in. The
-- algae tank stands four of them and the bio garden two, which is exactly
-- what each model carries.
return {
    type = "item",
    name = "culture-column",
    icon = "__LavaBlock-graphics__/graphics/icons/parts/culture-column.png",
    icon_size = 64,
    subgroup = "intermediate-product",
    order = "z[lavablock]-b[culture-column]",
    stack_size = 50,
}
